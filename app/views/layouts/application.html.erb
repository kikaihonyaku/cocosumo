<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "CoCoスモ - 不動産業務支援システム" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#1976d2">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Environment variables for frontend %>
    <meta name="google-maps-api-key" content="<%= ENV['VITE_GOOGLE_MAPS_API_KEY'] %>">
    <meta name="google-maps-map-id" content="<%= ENV['VITE_GOOGLE_MAPS_MAP_ID'] %>">

    <%= yield :head %>

    <link rel="preconnect" href="https://maps.googleapis.com">
    <link rel="preconnect" href="https://maps.gstatic.com" crossorigin>
    <link rel="icon" href="/icon.png?v=<%= Time.now.to_i %>" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png?v=<%= Time.now.to_i %>">
    <link rel="manifest" href="/manifest.json">

    <%# Vite + React (CSS is bundled with JS) %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application.jsx' %>
  </head>

  <body style="margin: 0; padding: 0;">
    <%= yield %>
  </body>
</html>
